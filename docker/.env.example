# docker-compose が読み込む環境変数。
# このファイルをコピーして docker/.env を作り、値を書き換えて使う。
#
#   cd docker
#   cp .env.example .env
#
# APP_TOKEN_SALT はCSRFトークン生成に使うsalt。
# 推測されない十分な長さの文字列を各自で生成すること。値はコミットしない。
#
#   openssl rand -hex 32
#
APP_TOKEN_SALT=
